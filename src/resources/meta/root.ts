import { type Metadata, type Viewport } from "next";

export const rootMetadata: Metadata = {
  title: "E-Com | Template",
  description: "This is a e-com template",
};

export const rootViewport: Viewport = {
  themeColor: "#000",
  viewportFit: "cover",
  minimumScale: 1,
  initialScale: 1,
  width: "device-width",
};
